<% @page_title = "New promo" %>
    <% if !flash[:error].blank? %>
      <div class="container alert alert-danger">
        <%=flash[:error]%>
      </div>
    <%end%>

<div class= "container">
  <%= form_for(@promo) do |f| %>
  <div class="row">
    <h2 class="form-signin-heading col-sm-4 col-md-offset-5">New promo</h2>
    </div>
    <%=render(:partial => "form", :locals => {:f=>f})%>
    
  <% end %>
</div>