 <!-- Page Content -->
  <div class="container">

    <div class="row">
      <div class=" col-md-6">
         <% if @item.image.attached? %>
            <%= image_tag(@item.image, id: "show_image")%>

  <%else%>
  <%= image_tag("placeholderplant", id: "show_image")%>
<% end %>

         
      </div>

      <div class="col-md-6">
        <h3 class="card-title"><%= @item.name %></h3>
        $<%= @item.unit_value %>
    <br >
         <span class="text-warning">&#9733; &#9733; &#9733; &#9733; &#9734;</span>
         4.0 stars
        <div style="padding: 10px;"></div>
        <p class="card-text"><%= @item.description %></p>
       
      </div>
    </div>
  </div>